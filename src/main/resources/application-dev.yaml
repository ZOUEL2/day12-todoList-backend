server:
  port: ${PORT:8888}

logging:
  level:
    com.oocl.springbootemployee: DEBUG
    org.springframework.web: INFO
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"